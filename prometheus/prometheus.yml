global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "4.255.72.143:9093" 
rule_files:
  - alertrules.yml

scrape_configs:
- job_name: 'Host'
  file_sd_configs:
  - files:
    - 'targets.json'